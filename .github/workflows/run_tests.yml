name: Tests

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:
    - uses: actions/checkout@v2

    - name: Install Ninja
      run: sudo apt-get install ninja-build

    - name: Install Eigen
      run: sudo apt-get install libeigen3-dev

    - name: Build
      run: bash build.sh -t

    - name: Test
      working-directory: ${{runner.workspace}}/n-fold-solver
      run: GTEST_OUTPUT=xml:build/test-results/ GTEST_COLOR=1 ./nFold_test -V

    - name: Upload test results
      uses: actions/upload-artifact@v2
      if: failure()
      with:
        name: test_results_xml
        path: ${{runner.workspace}}/n-fold-solver/build/test-results/**/*.xml
